# Copy to Vercel → Project → Settings → Environment Variables.
# Never commit the real password.
# Optional: SESSION_SECRET for signing the session cookie.
# If unset, cookies are signed with SITE_PASSWORD.

SITE_PASSWORD=
# SESSION_SECRET=
